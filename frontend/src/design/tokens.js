export const BrandTokens = {
  color: {
    text: "var(--text)",
    bg: "var(--bg)",
    lavender: "#BCA7E8",
    graphite: "#DADDE1",
  },
  font: {
    sans: "var(--font-questrial), system-ui, sans-serif",
  },
  shadow: {
    soft: "0 8px 24px rgba(0,0,0,0.35)",
  },
};

// Sizes (independent)
export const sizes = {
  // Header wordmark (NEST + orb) — controls TITLE ONLY
  headerTitle: "clamp(28px, 4.8vw, 70px)",

  // Hero wordmark (big lockup)
  heroTitle: "clamp(120px,14vw,240px)",

  // Avatar size — independent from title
  avatarClamp: "clamp(40px,5vw,70px)",
};